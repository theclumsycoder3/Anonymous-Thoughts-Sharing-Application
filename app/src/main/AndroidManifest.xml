<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SIHApplication"
        tools:targetApi="31">
        <activity
            android:name=".QuizFragment"
            android:exported="false" />
        <activity
            android:name=".LoginLayoutActivity"
            android:exported="true"
            android:label="Login Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterLayoutActivity"
            android:exported="false"
            android:label="Register Page" />
        <activity
            android:name=".SecondFragment"
            android:exported="false"
            android:label="Add your problem" />
        <activity
            android:name=".ThirdFragment"
            android:exported="false"
            android:label="See Comments" />
        <activity
            android:name=".MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>